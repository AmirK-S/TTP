---
phase: 26-onboarding-settings-ux
plan: '02'
type: execute
wave: 2
depends_on:
  - '26-01'
files_modified: []
autonomous: true
requirements:
  - SETX-01
  - SETX-02
  - SETX-03
must_haves:
  truths:
    - "Settings window opens automatically after API key is saved"
    - "Settings window appears in foreground (not behind other windows)"
    - "Tutorial pill shows correct shortcut (FN) on first launch"
  artifacts:
    - path: src-tauri/src/settings.rs
      provides: Settings window management with auto-show
      exports: open_settings_window, ensure_foreground
    - path: src/components/Settings.tsx
      provides: Settings component with API key save handler
      min_lines: 100
    - path: src/components/TutorialPill.tsx
      provides: Tutorial pill showing shortcut
      exports: showTutorialPill
  key_links:
    - from: src/components/Settings.tsx
      to: src-tauri/src/settings.rs
      via: invoke command
      pattern: open_settings_window
    - from: src/App.tsx
      to: src/components/TutorialPill.tsx
      via: import and render
      pattern: TutorialPill
---

<objective>
Settings UX improvements - auto-open after API key, foreground window, tutorial pill
</objective>

<execution_context>
@/Users/amirkellousidhoum/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/26-onboarding-settings-ux/26-01-PLAN.md

Prior decisions from STATE.md:
- Window focus: Call `show()` before `set_focus()` for auto-opened settings
- Settings persist across app restarts via JSON file

Tech stack: Tauri 2 + React + TypeScript
</context>

<tasks>

<task type="auto">
<name>Add auto-show settings on API key save</name>
  <files>src-tauri/src/settings.rs, src/components/Settings.tsx</files>
  <action>
1. In settings.rs: Add open_settings_window() command that:
   - Checks if settings window already exists
   - If exists: call show() then set_focus() (per prior decision)
   - If not: create new window with show() called immediately

2. In Settings.tsx: 
   - Add onSave handler that calls invoke('open_settings_window') after API key saves
   - Add state to track if this is first API key entry
  </action>
  <verify>
cargo check compiles, npm run build compiles
  </verify>
  <done>
Settings window opens automatically after API key is saved
</done>
</task>

<task type="auto">
<name>Ensure settings window appears in foreground</name>
  <files>src-tauri/src/settings.rs</files>
  <action>
In settings.rs:
- Update window creation to use proper focus sequence
- Call show() BEFORE set_focus() (critical order per prior decision)
- Add ensure_foreground() helper for existing windows
- Set window to skip taskbar, show in dock
  </action>
  <verify>
cargo check compiles
  </verify>
  <done>
Settings window appears in foreground when opened
</done>
</task>

<task type="auto">
<name>Create TutorialPill component for first launch</name>
  <files>src/components/TutorialPill.tsx</files>
  <action>
Create TutorialPill.tsx with:
- Props: shortcutText (default: "FN")
- Shows tooltip on first launch explaining the shortcut
- Dismissible (click to dismiss, stores in localStorage)
- Position: near the pill, bottom-center
- Content: "Press FN to start recording"
  </action>
  <verify>
npm run build compiles
  </verify>
  <done>
Tutorial pill displays FN shortcut on first launch
</done>
</task>

<task type="auto">
<name>Integrate tutorial pill into main app</name>
  <files>src/App.tsx</files>
  <action>
In App.tsx:
- Import TutorialPill component
- Show tutorial pill only when:
  1. First launch (check via Tauri command)
  2. Not dismissed in localStorage
- Dismiss on click, store in localStorage
  </action>
  <verify>
npm run build compiles
  </verify>
  <done>
Tutorial pill shows on first launch and can be dismissed
</done>
</task>

</tasks>

<verification>
1. Saving API key in settings opens settings window automatically
2. Settings window appears in front of other windows
3. Tutorial pill shows "FN" as shortcut on first launch
4. Tutorial pill can be dismissed
</verification>

<success_criteria>
- SETX-01: Settings opens after API key saved
- SETX-02: Settings appears in foreground
- SETX-03: Tutorial pill shows FN shortcut
</success_criteria>

<output>
After completion, create `.planning/phases/26-onboarding-settings-ux/26-02-SUMMARY.md`
</output>
