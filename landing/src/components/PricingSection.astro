---
import { getLangFromUrl, useTranslations } from '@/i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const buyUrl = 'https://amirks.lemonsqueezy.com/buy/dcc74241-21ae-4d20-8a3c-90bf8d842bae';

const freeFeatures = [
  t('pricing.free.feat1'),
  t('pricing.free.feat2'),
  t('pricing.free.feat3'),
  t('pricing.free.feat4'),
];

const proFeatures = [
  t('pricing.pro.feat1'),
  t('pricing.pro.feat2'),
  t('pricing.pro.feat3'),
  t('pricing.pro.feat4'),
];
---

<section id="pricing" data-snap-section data-section-pricing class="relative px-6 py-32 md:py-48">
  <div class="mx-auto max-w-5xl">
    <!-- Section heading -->
    <div data-pricing-heading class="mb-16 md:mb-20 text-center">
      <h2 class="text-heading font-bold tracking-tight">
        <span class="inline-block bg-gradient-to-r from-white to-slate-400 bg-clip-text text-transparent">
          {t('pricing.heading')}
        </span>
      </h2>
      <p class="mt-4 text-body-lg text-slate-400">
        {t('pricing.subheading')}
      </p>
    </div>

    <!-- Pricing grid: Free | Pro -->
    <div class="grid grid-cols-1 gap-6 md:grid-cols-2 md:gap-8" data-spotlight>
      <!-- Free tier -->
      <div data-pricing-card data-spotlight-card class="group relative overflow-hidden flex flex-col rounded-2xl border border-white/10 bg-white/[0.05] backdrop-blur-sm p-8 transition-colors hover:border-white/20 hover:bg-white/[0.07]">
        <!-- Tier label -->
        <div class="mb-2">
          <span class="text-sm font-semibold uppercase tracking-wider text-slate-400">
            {t('pricing.free.label')}
          </span>
        </div>

        <!-- Tier name -->
        <h3 class="mb-2 text-2xl font-bold text-white">
          {t('pricing.free.name')}
        </h3>

        <!-- Price -->
        <div class="mb-1 flex items-baseline gap-2">
          <span class="text-5xl font-extrabold text-white">
            {t('pricing.free.price')}
          </span>
        </div>
        <p class="mb-8 text-sm text-slate-400">
          {t('pricing.free.priceSub')}
        </p>

        <!-- Features list -->
        <ul class="mb-8 flex flex-col gap-3">
          {freeFeatures.map((feature) => (
            <li class="flex items-start gap-3">
              <svg class="mt-0.5 h-5 w-5 flex-shrink-0 text-slate-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="20 6 9 17 4 12" />
              </svg>
              <span class="text-sm leading-relaxed text-slate-300">{feature}</span>
            </li>
          ))}
        </ul>

        <!-- CTA (free) -->
        <div class="mt-auto">
          <a
            href="#install"
            class="block w-full rounded-full border border-white/10 bg-white/[0.03] px-6 py-3 text-center text-sm font-medium text-white transition-colors hover:bg-white/[0.08]"
          >
            {t('pricing.free.cta')}
          </a>
        </div>
      </div>

      <!-- Pro tier (highlighted) -->
      <div data-pricing-card data-pricing-pro class="group relative overflow-hidden rounded-2xl">
        <!-- Animated gradient border -->
        <div class="gradient-border-animated h-full">
          <div class="relative flex h-full flex-col rounded-[calc(1rem-1px)] bg-gradient-to-br from-violet-950/40 via-slate-950/95 to-slate-950/95 backdrop-blur-sm p-8" data-spotlight-card>
            <!-- "Recommended" badge -->
            <div class="absolute right-6 top-6">
              <span class="inline-flex items-center rounded-full border border-violet-500/30 bg-violet-500/10 px-3 py-1 text-xs font-semibold uppercase tracking-wider text-violet-300">
                {t('pricing.pro.badge')}
              </span>
            </div>

            <!-- Tier label -->
            <div class="mb-2">
              <span class="text-sm font-semibold uppercase tracking-wider text-violet-300">
                {t('pricing.pro.label')}
              </span>
            </div>

            <!-- Tier name -->
            <h3 class="mb-2 text-2xl font-bold text-white">
              {t('pricing.pro.name')}
            </h3>

            <!-- Price -->
            <div class="mb-1 flex items-baseline gap-2">
              <span class="text-5xl font-extrabold text-white">{`{{PRICE}}`}</span>
            </div>
            <p class="mb-8 text-sm text-slate-400">
              {t('pricing.pro.priceSub')}
            </p>

            <!-- Features list -->
            <ul class="mb-8 flex flex-col gap-3">
              {proFeatures.map((feature) => (
                <li class="flex items-start gap-3">
                  <svg class="mt-0.5 h-5 w-5 flex-shrink-0 text-violet-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                    <polyline points="20 6 9 17 4 12" />
                  </svg>
                  <span class="text-sm leading-relaxed text-slate-200">{feature}</span>
                </li>
              ))}
            </ul>

            <!-- CTA (Pro) -->
            <div class="mt-auto">
              <a
                href={buyUrl}
                target="_blank"
                rel="noopener"
                class="block w-full rounded-full bg-gradient-to-r from-violet-500 to-fuchsia-500 px-6 py-3 text-center text-sm font-semibold text-white shadow-lg shadow-violet-500/25 transition-all hover:from-violet-400 hover:to-fuchsia-400 hover:shadow-violet-500/40"
              >
                {t('pricing.pro.cta')}
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import gsap from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";
  gsap.registerPlugin(ScrollTrigger);

  const section = document.querySelector("[data-section-pricing]");
  if (section) {
    const mm = gsap.matchMedia();

    mm.add({
      isDesktop: "(min-width: 768px) and (prefers-reduced-motion: no-preference)",
      isMobile: "(max-width: 767px) and (prefers-reduced-motion: no-preference)",
      reduceMotion: "(prefers-reduced-motion: reduce)",
    }, (context) => {
      const { isDesktop, isMobile, reduceMotion } = context.conditions;
      if (reduceMotion) return;

      const heading = section.querySelector("[data-pricing-heading]");
      const cards = gsap.utils.toArray("[data-pricing-card]");

      if (isDesktop) {
        gsap.from(heading, {
          opacity: 0,
          y: 30,
          duration: 0.8,
          ease: "power3.out",
          scrollTrigger: {
            trigger: heading,
            start: "top 80%",
            once: true,
          },
        });

        gsap.from(cards, {
          opacity: 0,
          y: 60,
          duration: 0.8,
          ease: "power2.out",
          stagger: 0.15,
          scrollTrigger: {
            trigger: section,
            start: "top 60%",
            once: true,
          },
        });
      }

      if (isMobile) {
        gsap.from(heading, {
          opacity: 0,
          y: 20,
          duration: 0.6,
          scrollTrigger: { trigger: heading, start: "top 85%", once: true },
        });

        gsap.from(cards, {
          opacity: 0,
          y: 30,
          stagger: 0.15,
          duration: 0.6,
          ease: "power2.out",
          scrollTrigger: { trigger: cards[0] as Element, start: "top 85%", once: true },
        });
      }
    });
  }
</script>
