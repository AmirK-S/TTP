// TTP - Talk To Paste
// Main App component (hidden window for tray-only app)

/**
 * Main App component - this window is hidden by default.
 * TTP is a tray-only application, so this component serves
 * as a placeholder for the main window which remains invisible.
 */
function App() {
  // This window is hidden - TTP runs from the system tray
  return null;
}

export default App;
